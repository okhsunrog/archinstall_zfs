export component App inherits Window {
    background: #1e1e2e;
    preferred-width: 800px;
    preferred-height: 600px;

    VerticalLayout {
        alignment: center;
        padding: 20px;
        spacing: 16px;

        Text {
            text: "archinstall-zfs";
            color: #cdd6f4;
            font-size: 36px;
            horizontal-alignment: center;
        }

        Text {
            text: "Slint KMS UI — experimental";
            color: #a6adc8;
            font-size: 18px;
            horizontal-alignment: center;
        }
    }
}
